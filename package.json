{
  "name": "ledcenter",
  "version": "0.0.0",
  "private": true,
  "description": "Ledcenter.hu — magyar LED-vilagitastechnikai webshop (Medusa.js 2.x + Next.js 15)",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:unit": "pnpm -r test:unit",
    "test:e2e": "pnpm -r test:e2e"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
